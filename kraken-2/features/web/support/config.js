const config = require('../../../properties.json');

const GHOST_URL = config.GHOST_URL;
const USERNAME = config.USERNAME;
const PASSWORD = config.PASSWORD;

module.exports = {
  GHOST_URL,
  USERNAME,
  PASSWORD,
};
